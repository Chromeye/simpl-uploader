import { SimplUploaderDirective } from './simpl-uploader.directive';

describe('SimplUploaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SimplUploaderDirective();
    expect(directive).toBeTruthy();
  });
});
